[project]
name = "unorag-mcp"
version = "0.1.0"
description = "Thin MCP server adapter for UnoRAG Knowledge API v1 (retrieve / ask)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Proprietary" }
authors = [{ name = "UnoRAG" }]
keywords = ["unorag", "mcp", "rag", "knowledge", "retrieve", "ask"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Typing :: Typed",
]
dependencies = [
  "mcp>=1.9.0,<2",
  "unorag @ file:../python",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-asyncio>=0.24.0",
]

[project.scripts]
unorag-mcp = "unorag_mcp.server:main"

[project.urls]
Homepage = "https://github.com/codexlin/UnoRAG"
Documentation = "https://github.com/codexlin/UnoRAG/blob/main/sdk/mcp/README.md"
Contract = "https://github.com/codexlin/UnoRAG/blob/main/docs/contracts/retrieve-ask-v1.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["unorag_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
