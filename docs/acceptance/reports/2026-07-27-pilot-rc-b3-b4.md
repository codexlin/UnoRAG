# 试点 · B3 升级 + B4 回滚演练

> 绑定干净工作树上的脚本提交 **`88b72d9`**（含 `b3_b4_upgrade_rollback.sh`）。  
> 产品运行时基线仍可追溯 **RC2-X** `a79d2a53c5ecb32423dae179bdb05784af187a46`。  
> 结论：本项 **PASS**；整体仍为 **Conditional GO**（B5 告警未接通）。

## 元数据 / 审计绑定

| 字段 | 值 |
|---|---|
| **证据提交（脚本 + 干净复跑）** | `88b72d9daf7910c4eed9d51963d9e4e9ac7feb8c` |
| RC2-X（产品验收运行时基线） | `a79d2a53c5ecb32423dae179bdb05784af187a46` |
| 旧版本（API worktree） | `b98f01438045c92804204449d3172ceb201490e6`（RC1） |
| 新版本（API） | `88b72d9…`（= HEAD） |
| Web 镜像 | `meriknow-web:local`（无正式旧镜像标签；已在报告中写明） |
| `.b3_b4_last_run.json` sha256 | `5d27c590e1e524a9a3d571160bd98b0ac1e870a3760202e39826656294a4e6cd` |
| 运行时对齐 | `rc_sha == git_head == script_sha == 88b72d9…`；`git_status_porcelain=""` |
| 拓扑 | 一次性 Compose 独立项目 `meriknow-b3` / `meriknow-b3-restore` + bind 文档目录（**不**触碰 `.meriknow`） |
| 日期 | 2026-07-27 |

## 结果总览

| 阶段 | 结果 | 说明 |
|---|---|---|
| **B3** 升级 | **PASS** | 旧 API seed → 升级前备份 → migrate → 新 API → smoke + 一致性 |
| **B4A** 仅应用回滚 | **PASS** | 旧 API 接升级后 DB（schema 兼容：无 drizzle/rag migration diff） |
| **B4B** 数据恢复回滚 | **PASS** | 复用 B2 语义恢复升级前备份后，旧 API 全量校验通过 |
| 总体 | **PASS** | |

## 流程摘要

1. 独立 Postgres/Qdrant/Redis（端口 `15532/16433/16479`）+ 工作区文档目录  
2. 旧 API（`b98f014` worktree）+ `meriknow-web:local`：上传 → replace → restricted ACL → service key → Ask → archive  
3. 升级前备份：`postgres.sql` + `documents.tgz` + `qdrant.tgz` + `MANIFEST.txt`  
4. **B3**：migrate（`pnpm db:migrate` + `apply_rag_migrations`）→ 新 API（HEAD）→ 校验  
5. **B4A**：停新进程，拉起旧 API（同 DB）→ 校验  
6. **B4B**：销毁 volumes → restore 升级前备份 → 旧 API → 校验  

### 校验项（B3 / B4A / B4B 均覆盖）

health、对象文件、active version/generation 不变、ACL 未扩大、Ask/Retrieve + citation、Service Key、lifecycle inspect、job 状态摘要、**Qdrant↔PG exact match**。

本轮 Qdrant↔PG：`qdrant_count=2` / `pg_point_count=2`（org/ws/doc/version/generation/status=active）。

本地 JSON（勿提交；`0600`）：`scripts/acceptance/.b3_b4_last_run.json`（仅 `service_key_id` / `service_key_last4`）。

## 复跑

```bash
test -z "$(git status --porcelain)"
MERIKNOW_RC_SHA=88b72d9daf7910c4eed9d51963d9e4e9ac7feb8c \
  MERIKNOW_B3_NEW_SHA=88b72d9daf7910c4eed9d51963d9e4e9ac7feb8c \
  ./scripts/acceptance/b3_b4_upgrade_rollback.sh
```

脚本：[`../../../scripts/acceptance/b3_b4_upgrade_rollback.sh`](../../../scripts/acceptance/b3_b4_upgrade_rollback.sh)  
说明：[`../../../scripts/acceptance/README.md`](../../../scripts/acceptance/README.md)

## 与正式 GO 的关系

| 项 | 状态 |
|---|---|
| B3 升级演练 | **PASS**（本文） |
| B4 回滚演练 | **PASS**（B4A + B4B） |
| B5 容量/告警接通 | 见 `2026-07-27-pilot-rc-b5-min-alerts.md` |
| 正式 go/no-go 签字 | 仍待审批人 |
