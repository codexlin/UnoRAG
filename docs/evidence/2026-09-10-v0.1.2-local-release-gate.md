# UnoRAG v0.1.2 本地候选验收

- 日期：2026-09-10（Asia/Shanghai）
- 受测代码提交：`eb8d527facbeb99cecb8d35ec9bcbdf74369c816`
- 候选分支：`fix/v0.1.2-release-gate`
- 环境：全新隔离 Compose，`http://127.0.0.1:18080`
- 依赖：真实 PostgreSQL、Qdrant、Redis、DBOS、模型 Provider 与增强 MinerU Provider
- 结论：**本地发布门禁通过，可以进入 PR、主分支 CI 与不可变镜像发布阶段**

本报告证明受测提交及其本地配置边界，不等同于正式发布或目标客户环境 Production GO。Provider
凭据、管理员凭据和原始文档内容未写入报告。

## 本次修复

1. Generation cleanup 不再通过 `app.active_document_generations` 视图请求 `FOR SHARE`。Worker 继续
   使用 document advisory lock 与 `app.documents ... FOR UPDATE`，和 generation activation 保持相同
   锁顺序，同时兼容最小权限 Worker 数据库角色。
2. 空文本和非法 UTF-8 分别保留稳定错误码 `document_ingest_empty`、`document_parse_invalid`，并在
   最近错误详情中显示可执行的原因、恢复建议和关联标识。
3. 本地 Compose 与 Dockerfile 移除硬编码开发版本，运行时版本统一回落到 `package.json` 唯一事实源。

## 确定性与真实基础设施门禁

| 门禁 | 结果 |
|---|---|
| 静态/合同测试 | 216 passed，1 个环境依赖用例按设计 skipped，0 failed |
| TypeScript Core | 353 passed，24 个环境依赖用例按设计 skipped，0 failed |
| 独立集成环境 | 50/50 passed，0 skipped，0 failed |
| TypeScript / Biome | typecheck、447 文件 lint 全部通过 |
| 供应链 | 440 项许可证表达式、450 项 NOTICE、26 个素材/fixture 哈希全部通过 |
| 生产依赖审计 | npm 官方 advisory 端点，moderate 及以上 0 已知漏洞 |

独立集成环境使用临时 PostgreSQL、Qdrant 与 Redis，并覆盖数据库最小权限、Qdrant ACL 隔离、DBOS
重放、删除竞态、告警和原子替换。环境在测试后已销毁。

## 空环境产品链路

删除所有应用数据卷后从空环境执行一键安装，迁移、数据库角色配置、管理员 bootstrap、Web、DBOS
Worker、Control Worker、Qdrant 与生命周期巡检均启动成功。健康接口返回 `status=ok`，开发构建版本
正确显示为 `0.1.1-dev`；版本提交随后仅将唯一事实源提升为 `0.1.2`。

完整产品 smoke 两次通过：

`login -> library -> upload -> Ask -> Public Retrieve/Ask -> service-key scope/isolation -> replace -> delete`

第二次 smoke 在同时重启 Qdrant 与 DBOS Worker 后执行；服务约 3 秒恢复 ready，产品链路再次 PASS。
结束时 lifecycle 状态为 `dead=0`、`stuck=0`、`cleanup_errors=0`。

## 真实文件与质量门禁

增强 MinerU Provider 对全新临时知识库完成 7/7 入库：

- 长合同 DOCX；
- 80 行报价表 DOCX；
- 5K 叙事 Markdown；
- 跨页大表 PDF；
- 图表混排 PDF；
- 低对比扫描 PDF；
- 双栏 PDF。

同一不可变索引上的质量结果为：正例 `33/33`、拒答 `5/5`，Ingest Reliability Gate、Ask Gate 和
总 Release Gate 均为 PASS。临时验收知识库随后通过产品删除流程清理。

## 失败路径与可观测性

真实上传空白 Markdown 后，ingest 按预期失败，generation cleanup 完成并进入 `deleted`，数据库持久化
错误码为 `document_ingest_empty`。浏览器最近错误详情能够关联并展示：

- 稳定错误码、可读原因与恢复建议；
- 请求、workflow、job、document version 和 trace 标识；
- 原始脱敏错误详情；
- 入库阶段瀑布图和关键时间戳。

桌面浏览器实测知识库、文档详情、运维看板和错误详情。`390 x 844` 移动视口实测知识库、问答、
运维和操作记录页面，均无横向溢出或控件遮挡。

## 发布前剩余项

本地 GO 后仍须按顺序完成：

1. PR 与受保护主分支 CI；
2. 从合并后的精确 commit 创建 `v0.1.2` 标签；
3. 四镜像 Trivy、SBOM、provenance、Cosign 签名和 digest manifest 发布；
4. 对目标环境执行 digest-pinned 升级、健康检查、产品 smoke 与生命周期巡检。

任一步失败都不得把本报告升级为正式发布或 Production GO。
