name: ci

# P0 CI closed loop (build / test / scan-in-repo / image build verify).
# Does NOT push to registry, read production secrets, or SSH deploy.
# Release image push: see release-images.yml (requires ACR secrets for real push).
# Deploy with approval: deploy.yml (deferred).
#
# This workflow is the single PR/main entrypoint.

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  brand-residue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject legacy brand names
        run: ./scripts/check_brand_residue.sh

  lint-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reject conflict markers / whitespace errors
        run: git diff --check origin/main...HEAD || git diff --check HEAD~1...HEAD || git diff --check

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.7.1
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
      - name: TypeScript core contracts
        run: |
          pnpm typecheck
          pnpm test:ts-core
      - name: Test
        run: pnpm test
      - name: Lint
        run: pnpm lint
      - name: Production build
        run: pnpm build
        env:
          NEXT_TELEMETRY_DISABLED: "1"

  quality-scorer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Test live quality scorer
        run: python -m unittest discover -s tests -p 'test_run_ab_live_e2e.py'

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      # No type=gha cache: would need actions: write; P0 keeps contents: read only.
      - name: Build web runner (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/web.Dockerfile
          target: runner
          push: false
          load: true
          tags: unorag-web:ci
      - name: Build web migrator (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/web.Dockerfile
          target: migrator
          push: false
          load: true
          tags: unorag-web-migrator:ci
      - name: Build ops image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/web.Dockerfile
          target: ops
          push: false
          load: true
          tags: unorag-web-ops:ci
      - name: Build DBOS worker (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/docker/web.Dockerfile
          target: worker
          push: false
          load: true
          tags: unorag-web-worker:ci
