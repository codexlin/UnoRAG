UnoRAG {{ .Chart.AppVersion }}

Runtime workloads:
  - web: product UI, authentication, business APIs, Ask and Retrieval
  - dbos-worker: parsing, embedding, indexing and lifecycle workflows
  - dbos-control: durable dispatch and reconciliation

Required Secret keys:
  DATABASE_URL, WORKER_DATABASE_URL, DBOS_SYSTEM_DATABASE_URL,
  MIGRATOR_DATABASE_URL, UNORAG_SESSION_SECRET, LLM_API_KEY
