# MeriKnow private-deployment reference topology (L8).
#
# Config model (split by lifecycle / sensitivity):
#   ../config/runtime.env      — non-secret (ConfigMap)
#   ../config/runtime.secret   — secrets (Secret)
#   ../config/bootstrap.env    — one-time bootstrap Job only
#
# Init once:
#   ./scripts/init-config.sh
#
# Always use the helpers. They load --env-file without sourcing secrets and
# remove managed host variables that would otherwise override those files:
#   source scripts/compose-env.sh
#   mk_compose up -d
# Bootstrap/migrate profile:
#   mk_compose_bootstrap --profile migrate run --rm bootstrap
# mk_compose does NOT require bootstrap.env; mk_compose_bootstrap does.
#
# Browser → Caddy :${HTTP_PORT} → web :3000
# FastAPI / lifecycle-worker are NOT published.

name: ${COMPOSE_PROJECT_NAME:-meriknow}

services:
  # Named volumes default to root:root; web runs as uid 10001 (meriknow).
  document-storage-init:
    image: ${MERIKNOW_WEB_IMAGE:-meriknow-web:local}
    user: "0:0"
    volumes:
      - document_storage:/var/lib/meriknow/documents
    entrypoint: ["sh", "-c"]
    command:
      - "chown -R meriknow:meriknow /var/lib/meriknow/documents && chmod 775 /var/lib/meriknow/documents"
    restart: "no"
    networks:
      - internal

  caddy:
    image: ${CADDY_IMAGE:-caddy:2.9.1-alpine}
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    depends_on:
      web:
        condition: service_healthy
    networks:
      - edge
      - internal

  web:
    image: ${MERIKNOW_WEB_IMAGE:-meriknow-web:local}
    build:
      context: ../..
      dockerfile: deploy/docker/web.Dockerfile
      target: runner
    restart: unless-stopped
    environment:
      NODE_ENV: production
      RAG_API_URL: http://api:8000
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL required}
      DATABASE_POOL_MAX: ${DATABASE_POOL_MAX:-10}
      MERIKNOW_INTERNAL_SECRET: ${MERIKNOW_INTERNAL_SECRET:?MERIKNOW_INTERNAL_SECRET required}
      MERIKNOW_SESSION_SECRET: ${MERIKNOW_SESSION_SECRET:?MERIKNOW_SESSION_SECRET required}
      # Container-internal path only (named volume). Do not pass host absolute paths.
      DOCUMENT_STORAGE_ROOT: /var/lib/meriknow/documents
      DOCUMENT_MAX_UPLOAD_BYTES: ${DOCUMENT_MAX_UPLOAD_BYTES:-52428800}
    volumes:
      - document_storage:/var/lib/meriknow/documents
    depends_on:
      document-storage-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fsS http://127.0.0.1:3000/api/rag/health >/dev/null || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 40s
    networks:
      - internal
      - edge

  api:
    image: ${MERIKNOW_API_IMAGE:-meriknow-api:local}
    build:
      context: ../..
      dockerfile: deploy/docker/api.Dockerfile
    restart: unless-stopped
    environment:
      APP_ENV: ${APP_ENV:-production}
      APP_NAME: MeriKnow API
      API_PREFIX: /v1
      CORS_ORIGINS: http://localhost,http://127.0.0.1
      # Product invariants (not customer-tunable env knobs)
      ASK_MODE: live
      METADATA_BACKEND: postgres
      INTERNAL_AUTH_ENABLED: "true"
      INTERNAL_AUTH_SECRET: ${MERIKNOW_INTERNAL_SECRET:?MERIKNOW_INTERNAL_SECRET required}
      INTERNAL_AUTH_REPLAY_BACKEND: redis
      ACTIVE_GENERATION_GATE_ENABLED: "true"
      ACTIVE_GENERATION_CACHE_TTL_SECONDS: "0"
      MINERU_USE_FAKE: "false"
      DATABASE_URL: ${API_DATABASE_URL:?API_DATABASE_URL required}
      RAG_READ_DATABASE_URL: ${RAG_READ_DATABASE_URL:?RAG_READ_DATABASE_URL required}
      QDRANT_URL: ${QDRANT_URL:?QDRANT_URL required}
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-meriknow_chunks}
      REDIS_URL: ${REDIS_URL:?REDIS_URL required}
      DOCUMENT_STORAGE_ROOT: /var/lib/meriknow/documents
      MAX_UPLOAD_BYTES: ${DOCUMENT_MAX_UPLOAD_BYTES:-52428800}
      # Transitional mapping: deploy name LLM_* → app OPENAI_*
      OPENAI_API_KEY: ${LLM_API_KEY:?LLM_API_KEY required}
      OPENAI_BASE_URL: ${LLM_BASE_URL:?LLM_BASE_URL required}
      CHAT_MODEL: ${CHAT_MODEL:?CHAT_MODEL required}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:?EMBEDDING_MODEL required}
      EMBEDDING_DIM: ${EMBEDDING_DIM:?EMBEDDING_DIM required}
      EMBEDDING_BATCH_SIZE: ${EMBEDDING_BATCH_SIZE:-10}
      LLM_MAX_INFLIGHT: ${LLM_MAX_INFLIGHT:-4}
      RERANK_BASE_URL: ${RERANK_BASE_URL:-}
      RERANK_MODEL: ${RERANK_MODEL:-qwen3-rerank}
      MINERU_ENABLED: ${MINERU_ENABLED:-false}
      MINERU_PROVIDER: ${MINERU_PROVIDER:-self_hosted}
      MINERU_SELF_HOSTED_URL: ${MINERU_SELF_HOSTED_URL:-}
      MINERU_URL: ${MINERU_URL:-}
      MINERU_MODE: ${MINERU_MODE:-auto}
      MINERU_VERSION: ${MINERU_VERSION:-2.5}
      MINERU_PARSE_METHOD: ${MINERU_PARSE_METHOD:-auto}
      EXTERNAL_PARSER_ALLOWED: ${EXTERNAL_PARSER_ALLOWED:-false}
      MINERU_302_BASE_URL: ${MINERU_302_BASE_URL:-https://api.302.ai}
      MINERU_302_UPLOAD_PATH: ${MINERU_302_UPLOAD_PATH:-/302/upload-file}
      MINERU_302_TASK_PATH: ${MINERU_302_TASK_PATH:-/302/v2/mineru/task}
      MINERU_302_POLL_INTERVAL_S: ${MINERU_302_POLL_INTERVAL_S:-5}
      MINERU_302_MAX_WAIT_S: ${MINERU_302_MAX_WAIT_S:-900}
      LIFECYCLE_WORKER_READY_FILE: /tmp/meriknow-lifecycle-ready
      LIFECYCLE_WORKER_POLL_SECONDS: ${LIFECYCLE_WORKER_POLL_SECONDS:-1}
      LIFECYCLE_WORKER_LEASE_SECONDS: ${LIFECYCLE_WORKER_LEASE_SECONDS:-120}
      LIFECYCLE_WORKER_HEARTBEAT_SECONDS: ${LIFECYCLE_WORKER_HEARTBEAT_SECONDS:-30}
    volumes:
      - document_storage:/var/lib/meriknow/documents
    depends_on:
      document-storage-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fsS http://127.0.0.1:8000/health | grep -q '\"status\"' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    # Intentionally no ports: — FastAPI is internal-only (fail-closed edge).
    networks:
      - internal

  lifecycle-worker:
    image: ${MERIKNOW_API_IMAGE:-meriknow-api:local}
    build:
      context: ../..
      dockerfile: deploy/docker/api.Dockerfile
    restart: unless-stopped
    command: ["python", "-m", "app.lifecycle_worker"]
    environment:
      APP_ENV: ${APP_ENV:-production}
      # Invariants required by Settings production validator
      ASK_MODE: live
      METADATA_BACKEND: postgres
      INTERNAL_AUTH_ENABLED: "true"
      INTERNAL_AUTH_SECRET: ${MERIKNOW_INTERNAL_SECRET:?MERIKNOW_INTERNAL_SECRET required}
      INTERNAL_AUTH_REPLAY_BACKEND: redis
      ACTIVE_GENERATION_GATE_ENABLED: "true"
      ACTIVE_GENERATION_CACHE_TTL_SECONDS: "0"
      MINERU_USE_FAKE: "false"
      WORKER_DATABASE_URL: ${WORKER_DATABASE_URL:?WORKER_DATABASE_URL required}
      RAG_READ_DATABASE_URL: ${RAG_READ_DATABASE_URL:?RAG_READ_DATABASE_URL required}
      # Settings.database_url fallback; worker DSN is authoritative via WORKER_*
      DATABASE_URL: ${WORKER_DATABASE_URL:?WORKER_DATABASE_URL required}
      QDRANT_URL: ${QDRANT_URL:?QDRANT_URL required}
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-meriknow_chunks}
      REDIS_URL: ${REDIS_URL:?REDIS_URL required}
      DOCUMENT_STORAGE_ROOT: /var/lib/meriknow/documents
      MAX_UPLOAD_BYTES: ${DOCUMENT_MAX_UPLOAD_BYTES:-52428800}
      OPENAI_API_KEY: ${LLM_API_KEY:?LLM_API_KEY required}
      OPENAI_BASE_URL: ${LLM_BASE_URL:?LLM_BASE_URL required}
      CHAT_MODEL: ${CHAT_MODEL:?CHAT_MODEL required}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:?EMBEDDING_MODEL required}
      EMBEDDING_DIM: ${EMBEDDING_DIM:?EMBEDDING_DIM required}
      EMBEDDING_BATCH_SIZE: ${EMBEDDING_BATCH_SIZE:-10}
      LLM_MAX_INFLIGHT: ${LLM_MAX_INFLIGHT:-4}
      MINERU_ENABLED: ${MINERU_ENABLED:-false}
      MINERU_PROVIDER: ${MINERU_PROVIDER:-self_hosted}
      MINERU_SELF_HOSTED_URL: ${MINERU_SELF_HOSTED_URL:-}
      MINERU_URL: ${MINERU_URL:-}
      MINERU_MODE: ${MINERU_MODE:-auto}
      MINERU_VERSION: ${MINERU_VERSION:-2.5}
      MINERU_PARSE_METHOD: ${MINERU_PARSE_METHOD:-auto}
      EXTERNAL_PARSER_ALLOWED: ${EXTERNAL_PARSER_ALLOWED:-false}
      MINERU_302_API_KEY: ${MINERU_302_API_KEY:-}
      MINERU_302_BASE_URL: ${MINERU_302_BASE_URL:-https://api.302.ai}
      MINERU_302_UPLOAD_PATH: ${MINERU_302_UPLOAD_PATH:-/302/upload-file}
      MINERU_302_TASK_PATH: ${MINERU_302_TASK_PATH:-/302/v2/mineru/task}
      MINERU_302_POLL_INTERVAL_S: ${MINERU_302_POLL_INTERVAL_S:-5}
      MINERU_302_MAX_WAIT_S: ${MINERU_302_MAX_WAIT_S:-900}
      LIFECYCLE_WORKER_READY_FILE: /tmp/meriknow-lifecycle-ready
      LIFECYCLE_WORKER_POLL_SECONDS: ${LIFECYCLE_WORKER_POLL_SECONDS:-1}
      LIFECYCLE_WORKER_LEASE_SECONDS: ${LIFECYCLE_WORKER_LEASE_SECONDS:-120}
      LIFECYCLE_WORKER_HEARTBEAT_SECONDS: ${LIFECYCLE_WORKER_HEARTBEAT_SECONDS:-30}
      LIFECYCLE_LOCAL_CAPACITY: ${LIFECYCLE_LOCAL_CAPACITY:-2}
      LIFECYCLE_MINERU_CAPACITY: ${LIFECYCLE_MINERU_CAPACITY:-1}
    volumes:
      - document_storage:/var/lib/meriknow/documents
    depends_on:
      document-storage-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "test -f /tmp/meriknow-lifecycle-ready || exit 1",
        ]
      interval: 10s
      timeout: 3s
      retries: 12
      start_period: 20s
    stop_grace_period: 2m
    networks:
      - internal

  postgres:
    image: ${POSTGRES_IMAGE:-postgres:17-alpine}
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-meriknow}
      POSTGRES_USER: ${POSTGRES_USER:-meriknow}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-meriknow} -d ${POSTGRES_DB:-meriknow}",
        ]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - internal

  qdrant:
    image: ${QDRANT_IMAGE:-qdrant/qdrant:v1.13.2}
    restart: unless-stopped
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333'",
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    networks:
      - internal

  redis:
    image: ${REDIS_IMAGE:-redis:7-alpine}
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - internal

  migrate-web:
    profiles: ["migrate"]
    # Distinct tag so migrator layers are not overwritten by the runtime web image.
    image: ${MERIKNOW_WEB_MIGRATOR_IMAGE:-meriknow-web-migrator:local}
    build:
      context: ../..
      dockerfile: deploy/docker/web.Dockerfile
      target: migrator
    environment:
      DATABASE_URL: ${MIGRATOR_DATABASE_URL:?MIGRATOR_DATABASE_URL required}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
    restart: "no"

  migrate-rag:
    profiles: ["migrate"]
    image: ${MERIKNOW_API_IMAGE:-meriknow-api:local}
    build:
      context: ../..
      dockerfile: deploy/docker/api.Dockerfile
    working_dir: /app
    environment:
      MIGRATOR_DATABASE_URL: ${MIGRATOR_DATABASE_URL:?MIGRATOR_DATABASE_URL required}
      PYTHONPATH: /app
    command: ["python", "scripts/apply_rag_migrations.py"]
    depends_on:
      postgres:
        condition: service_healthy
      migrate-web:
        condition: service_completed_successfully
    networks:
      - internal
    restart: "no"

  bootstrap:
    profiles: ["migrate"]
    image: ${MERIKNOW_WEB_MIGRATOR_IMAGE:-meriknow-web-migrator:local}
    build:
      context: ../..
      dockerfile: deploy/docker/web.Dockerfile
      target: migrator
    working_dir: /repo/apps/web
    environment:
      # Bootstrap-only vars: use defaults so `compose config` works without
      # bootstrap.env. The bootstrap script still requires a real password at run.
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL required}
      MERIKNOW_ORGANIZATION_ID: ${MERIKNOW_ORGANIZATION_ID:-00000000-0000-4000-8000-000000000001}
      MERIKNOW_WORKSPACE_ID: ${MERIKNOW_WORKSPACE_ID:-00000000-0000-4000-8000-000000000002}
      MERIKNOW_PRINCIPAL_ID: ${MERIKNOW_PRINCIPAL_ID:-00000000-0000-4000-8000-000000000003}
      MERIKNOW_ORGANIZATION_SLUG: ${MERIKNOW_ORGANIZATION_SLUG:-default}
      MERIKNOW_ORGANIZATION_NAME: ${MERIKNOW_ORGANIZATION_NAME:-MeriKnow Private Deployment}
      MERIKNOW_WORKSPACE_SLUG: ${MERIKNOW_WORKSPACE_SLUG:-default}
      MERIKNOW_WORKSPACE_NAME: ${MERIKNOW_WORKSPACE_NAME:-Default Workspace}
      MERIKNOW_ADMIN_SUBJECT: ${MERIKNOW_ADMIN_SUBJECT:-local-admin}
      MERIKNOW_ADMIN_EMAIL: ${MERIKNOW_ADMIN_EMAIL:-admin@example.com}
      MERIKNOW_ADMIN_NAME: ${MERIKNOW_ADMIN_NAME:-Local Administrator}
      MERIKNOW_ADMIN_PASSWORD: ${MERIKNOW_ADMIN_PASSWORD:-}
      MERIKNOW_ADMIN_PASSWORD_UPSERT: ${MERIKNOW_ADMIN_PASSWORD_UPSERT:-}
    command: ["node", "scripts/bootstrap-control-plane.mjs"]
    depends_on:
      migrate-rag:
        condition: service_completed_successfully
    networks:
      - internal
    restart: "no"

networks:
  # Edge: reverse proxy only. Do not attach FastAPI here.
  edge:
    driver: bridge
  # App backend. Ports for api/worker are unpublished (network isolation via
  # non-publish, not Docker "internal: true") so the data plane can still reach
  # customer LLM / embedding / MinerU endpoints.
  internal:
    driver: bridge

volumes:
  postgres_data:
  qdrant_data:
  redis_data:
  document_storage:
